interface:
  display_name: "BigBang"
  short_description: "Architecture-frozen InnovationLab full-pack builder with automatic session state, per-session Git checkpoints, recovery, and validated demo-ready delivery."
  default_prompt: "Use $bigbang to build a complete source-aligned InnovationLab package with mandatory architecture freeze, start-from-scratch entrance, automatic bounded-session execution, stateful TASKS.md, per-session Git commits, pause/resume, validation, and demoable final-system gate."
